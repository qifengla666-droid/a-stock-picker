FROM node:18-alpine

WORKDIR /app

# 复制package文件
COPY package*.json ./

# 安装依赖
RUN npm ci --prefer-offline --no-audit

# 复制应用代码
COPY . .

# 构建应用
RUN npm run build

# 安装serve来提供静态文件
RUN npm install -g serve

# 暴露端口
EXPOSE 3000

# 启动应用
CMD ["serve", "-s", "build", "-l", "3000"]
